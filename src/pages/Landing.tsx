import { Navbar, Hero } from '@components';

import { Sponsorship } from '@components';

export const Landing = () => {
    return (
        <div>
            <main>
                <div id="sponsorship">
                    <Sponsorship />
                </div>
            </main>
        </div>
    );
};
